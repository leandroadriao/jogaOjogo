<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="labelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="Small" />
                <Setter Property="TextColor" Value="#404040" />
            </Style>
            <Style x:Key="fieldStyle" TargetType="Label">
                <Setter Property="FontSize" Value="Medium" />
                <Setter Property="Margin" Value="10,0,0,0" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <ScrollView VerticalOptions="FillAndExpand">
                <StackLayout x:Name="ListaTimes"  Padding="10,10,10,10" HorizontalOptions="Start">
                    <Label Text="Time: 1" Style="{StaticResource labelStyle}" />
                    <Label Text="Leandro" Style="{StaticResource fieldStyle}" />

                </StackLayout>
            </ScrollView>
            <Button x:Name="navigateButton" Text="Previous Page" Clicked="OnNavigateButtonClicked" Grid.Row="4"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>